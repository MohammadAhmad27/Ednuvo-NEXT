import VerificationTest from "@/components/VerificationTest";

const page = () => {
  return (
    <div className="w-screen h-screen px-8 bg-secondary flex justify-center items-center">
      <VerificationTest />
    </div>
  );
};

export default page;
