import ResolutionCenter from "@/components/ResolutionCenter";

const page = () => {
  return (
    <div className="w-full overflow-hidden">
      <ResolutionCenter />
    </div>
  );
};

export default page;
